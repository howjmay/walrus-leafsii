// Test setup file
// import { vi } from 'vitest' // Unused but may be needed for future test mocks

// Mock environment variables for testing
process.env.VITE_API_BASE = 'http://localhost:8080'
process.env.NODE_ENV = 'test'

// Global test utilities and mocks can be added here